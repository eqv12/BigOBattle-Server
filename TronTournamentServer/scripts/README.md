# Scripts Layout

- active/: scripts still useful in current workflow
- legacy/: older scripts kept for reference/backward compatibility

Current policy:
- Keep root scripts for now to avoid breaking old commands.
- New work should use scripts/active and service entrypoints under server/.
